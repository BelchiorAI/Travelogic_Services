# Copy to .env.local (git-ignored) to run against the real Supplier API.
# Start the API first: in supplier-management, run `docker compose up`.

VITE_API_BASE_URL=http://localhost:5000

# true = in-browser mock data (the default, used by the Lovable preview); false = call the API above.
VITE_USE_MOCKS=false
